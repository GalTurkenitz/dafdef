# מפרט בנייה מלא: אפליקציית "קרא כדי לפתוח" — דמו דפדפן מוכן לבדיקה

> מסמך זה הוא מקור האמת של הפרויקט. המטרה: בסוף הבנייה לפי המסמך הזה יש דמו שלם, מעוצב ועובד, שנבדק על אייפון בספארי — בלי החלטות פתוחות. כל סטייה מהמפרט — לשאול לפני.

---

## 1. הקונספט
אפליקציה שחוסמת אפליקציות ממכרות (אינסטגרם, טיקטוק וכו') ופותחת אותן רק תמורת קריאת ספרים בעברית. קוראים עמודים ⇐ צוברים דקות מסך ⇐ הדקות מנוקזות כשמשתמשים באפליקציות החסומות.

שלב נוכחי: **דמו מלא שרץ בדפדפן** — ישמש כמפרט מדויק לגרסת ה-iOS וכדמו שיווקי. החסימה בדמו מדומה (אפליקציות מצוירות); החסימה האמיתית (Screen Time API / Swift) תיבנה בהמשך על Mac ואינה חלק מהפרויקט הזה.

שם עבודה: **"דפדף"** (placeholder — קל להחלפה, לשמור את השם כקבוע אחד ב-config).

## 2. סטאק וכללי עבודה
- Vanilla HTML/CSS/JS בלבד, אתר סטטי, בלי פריימוורקים ובלי build step. ספרייה חיצונית יחידה: epub.js (גרסה נעוצה 0.3.93 מ-jsDelivr; היא אינה קיימת ב-cdnjs)
- פריסה: Netlify
- Mobile-first: רוחב ייחוס 390px (iPhone). נבדק בספארי iOS על מכשיר אמיתי — כולל מגע, גלילה, safe-area
- עברית מלאה, `dir="rtl"` על הכל; אין טקסט ממשק באנגלית
- כל ה-state ב-localStorage (אין שרת, אין חשבונות)
- הלוגיקה העסקית — מודולי JS טהורים (ES modules) בלי שום תלות ב-DOM, כדי שיעברו כמו שהם ל-iOS
- גיט מהקומיט הראשון; קומיטים קטנים לפי סדר הבנייה (סעיף 10)

### מבנה תיקיות
```
/index.html            ⇐ מסך בית (טלפון מדומה)
/onboarding.html       ⇐ שאלון + תוצאה + בחירת אפליקציות
/reader.html           ⇐ הקורא
/library.html          ⇐ הספרייה
/dashboard.html        ⇐ דשבורד
/settings.html         ⇐ הגדרות
/css/tokens.css        ⇐ משתני עיצוב (סעיף 3) — קובץ יחיד לכל הצבעים/מידות
/css/app.css           ⇐ סגנונות משותפים ורכיבים
/js/logic/formula.js   ⇐ נוסחת שווי עמוד
/js/logic/bank.js      ⇐ בנק הזמן
/js/logic/verify.js    ⇐ אימות קריאה
/js/logic/streak.js    ⇐ סטריק
/js/logic/store.js     ⇐ עטיפת localStorage (סעיף 4)
/js/ui/…               ⇐ קוד מסכים
/content/catalog.json  ⇐ קטלוג בן-יהודה
/content/works/…       ⇐ קבצי יצירות (cache מקומי בזמן פיתוח)
/scripts/fetch-benyehuda.mjs ⇐ סקריפט משיכת התוכן (Node)
```

## 3. מערכת עיצוב (Design System)
העיקרון: שקט, ספרותי, חם. האפליקציה מתחרה בטיקטוק — היא צריכה להרגיש כמו ההפך ממנו: נייר, לא ניאון. בלי גרדיאנטים צעקניים, בלי צללים כבדים.

### צבעים — כמשתני CSS ב-tokens.css
מצב בהיר (ברירת מחדל):
- `--bg: #FAF6EF` (שמנת-נייר) · `--surface: #FFFFFF` · `--text: #1F2933` · `--text-soft: #5B6672`
- `--primary: #1F5F52` (ירוק ספרייה עמוק) · `--primary-contrast: #FFFFFF`
- `--accent: #C9862B` (ענבר — לסטריק ולחגיגות בלבד) · `--danger: #B3403A`
- `--border: #E5DED2` · `--lock: #2B2F36` (רקע מסך נעילה)

מצב כהה: `--bg: #14181D` · `--surface: #1D232B` · `--text: #ECEFF3` · `--text-soft: #9AA5B1` · `--primary: #3E8C7A` · `--border: #2A323C`

מצב ספיה (בקורא בלבד): `--bg: #F4ECD8` · `--text: #433422`

מעבר מצבים: class על `<html>` (`theme-light` / `theme-dark` / `theme-sepia`), ברירת מחדל לפי `prefers-color-scheme`.

### טיפוגרפיה
- ממשק: **Heebo** (Google Fonts) — משקלים 400/500/700
- טקסט קריאה בקורא: **Frank Ruhl Libre** — 400/500
- סולם: h1 28px/700 · h2 22px/700 · גוף 17px/400 · משני 15px · קטן 13px. שורה 1.6 בגוף, 1.8 בקורא
- מספרים גדולים (בנק, סטריק): Heebo 700, 44px, `font-variant-numeric: tabular-nums`

### מידות ורכיבים
- Spacing בכפולות 4: 4/8/12/16/24/32 · שולי מסך 20px · `border-radius`: כרטיס 16px, כפתור 14px, שבב 999px
- כפתור ראשי: גובה 52px, רוחב מלא, `--primary`, טקסט 17px/500. משני: מתאר `--border` על `--surface`. אזור מגע מינימלי לכל אלמנט: 44px
- כרטיס: `--surface`, radius 16, צל עדין `0 1px 3px rgba(0,0,0,.06)`
- **טבעת התקדמות (רכיב מרכזי):** SVG עגול שמציג את יתרת הדקות — מלא ב-`--primary`, מספר הדקות במרכז. משמש בבית ובדשבורד
- שבב סטריק: 🔥 + מספר, רקע `--accent` שקוף 12%
- אנימציות: עדינות וקצרות — 200-250ms ease-out למעברי מסך, "פליק" קטן על +דקות שנצברו. בלי קונפטי חוץ מרגע סיום ספר

### RTL
- הכל `dir="rtl"`; אייקוני חצים מתהפכים; דפדוף בקורא: עמוד הבא = החלקה שמאלה
- אייקונים: ספריית קו אחידה (Lucide כ-SVG מוטמע), עובי 1.75

## 4. מודל נתונים (localStorage, דרך store.js בלבד)
מפתחות תחת קידומת `dafdef:` — כולם JSON:
- `settings` — `{ pageValueMinutes, resetMode: "midnight"|"keep", theme, fontSize, onboardingDone }`
- `profile` — תשובות השאלון `{ screenTime, goal, readingHabit, strictness, resetChoice, createdAt }`
- `bank` — `{ minutes, lastUpdate }` (דקות ביתרה; עדכון עם timestamp לחישוב ניקוז וחצות)
- `blockedApps` — `[{ id, name, icon }]` (מדומה)
- `readingState` — `{ workId, location, pagesToday, minutesToday, date }`
- `streak` — `{ current, best, lastReadDate }`
- `stats` — מערך 7 ימים אחרונים `{ date, pagesRead, minutesEarned, minutesSpent }`
- `worksCache:<id>` — טקסט יצירה שהורדה

חוקי חצות: בכל טעינת עמוד/פוקוס, אם `date` ≠ היום ⇐ לסגור את היום הקודם (עדכון streak+stats), לאפס מונים יומיים, ולאפס `bank.minutes` רק אם `resetMode === "midnight"`.

## 5. הלוגיקה (מודולים — כולל חוזי פונקציות)

### formula.js — שווי עמוד
```
computePageValue({goal, strictness, readingHabit, daysSinceStart}) ⇐ דקות לעמוד
```
- בסיס לפי מטרה: לצמצם דרסטית = 10 · לאזן = 15 · בעיקר לקרוא יותר = 20
- מקדם קשיחות: רך ×1.25 · בינוני ×1.0 · **אכזרי ×0.5**
- "בכלל לא קורא": בשבועיים הראשונים ×1.25, דועך לינארית ל-×1.0 עד יום 14
- עיגול לחצי דקה. תוצאה נשמרת ב-`settings.pageValueMinutes` ומוצגת למשתמש עם הסבר מילולי ("בגלל שבחרת X — כל עמוד שווה Y דקות")

### bank.js — בנק הזמן
- `earn(pages)` ⇐ מוסיף `pages × pageValueMinutes`; אין תקרה
- `spendTick()` ⇐ בזמן ש"אפליקציה" פתוחה, מוריד בזמן אמת (עדכון כל שנייה, חישוב לפי delta של timestamps — עמיד למעבר לרקע)
- `bank.minutes === 0` ⇐ אירוע "ננעל" — סגירת האפליקציה המדומה וחזרה למסך נעילה
- מצב איפוס לפי `settings.resetMode` (חוקי חצות בסעיף 4)

### verify.js — אימות קריאה (שתי שכבות, שתיהן חובה)
1. **זמן מינימלי פר עמוד לפי מילים:** ספירת מילים בעמוד המוצג; קצב ייחוס 170 מילים/דקה; מינימום = `words/170` דקות, רצפה 20 שניות. דפדוף מוקדם ⇐ העמוד לא נספר + טוסט עדין "לאט לאט 🙂 עוד רגע העמוד נספר"
2. **נוכחות:** דפדוף ידני בלבד; אין מגע/אינטראקציה מעל 2.5 דקות ⇐ טיימר העמוד נעצר (חוזר במגע); `visibilitychange` (מעבר אפליקציה) ⇐ עצירה מיידית
- עמוד שעבר את שניהם ⇐ `bank.earn(1)` + אנימציית "+Y דק'" ליד טבעת ההתקדמות

### streak.js
- יום עם ≥1 עמוד מאומת = יום סטריק. `current`, `best`. פספוס יום ⇐ איפוס current. מוצג בבית ובדשבורד

## 6. ספריית תוכן — ספרים
**האפליקציה היא אפליקציה של ספרים.** לא שירים בודדים, לא מאמרים, לא מכתבים — ספרים.

- **מקור ראשי: פרויקט בן-יהודה** (4,129 כותרים). ה-API דורש מפתח חינמי שמונפק בשירות עצמי ב-`benyehuda.org/api_keys/new`, ומוגבל ל-50 בקשות בדקה.
- **המפתח לעולם לא נכנס לקוד הלקוח.** `scripts/fetch-benyehuda.mjs` רץ מקומית בלבד, קורא את המפתח מ-`~/.secrets/benyehuda.txt`, ומוריד גם את הקטלוג וגם את הטקסטים לקבצים ב-`content/`. האתר עצמו סטטי וקורא רק קבצים — בלי מפתח, בלי שרת, בלי תלות ברשת בזמן קריאה.
- נרמול לפורמט אחיד `{ id, title, author, genre, wordCount, estMinutes, url }` ⇐ `content/catalog.json`
- טקסט יצירה נשמר ב-`worksCache:<id>` בלחיצה הראשונה, לקריאה offline
- **מקור משני זמני:** עד שיש מפתח, `scripts/fetch-wikisource.mjs` מושך את מה שאפשר מוויקיטקסט העברי (נחלת הכלל). זה מקור דל בספרות — הוא קיים רק כדי שיהיה על מה לפתח ולבדוק, ומוחלף ברגע שהמפתח מגיע.
- מסך הספרייה: **חיפוש הוא רכיב מרכזי** — המשתמש מחפש ספר ומוצא אותו. בנוסף סינון לפי מחבר/סוגה, כרטיס יצירה (שם, מחבר, זמן משוער), **לחיצה ⇐ נפתח מיד בקורא**
- ספרים שהמשתמש מביא בעצמו: ייבוא EPUB בקורא (סעיף 7.5)

## 7. מפרט מסכים (כולם RTL, לפי מערכת העיצוב)

### 7.1 Onboarding — שאלון (5 מסכים + תוצאה)
מסך פר שאלה: פס התקדמות עליון (1/5…), שאלה כ-h1, תשובות ככרטיסים ברוחב מלא (בחירה אחת, מעבר אוטומטי אחרי 250ms):
1. כמה זמן מסך יש לך ביום? — פחות משעתיים / 2-4 / 4-6 / יותר מ-6
2. מה המטרה שלך? — לצמצם דרסטית / לאזן / בעיקר לקרוא יותר
3. כמה אתה קורא היום? — בכלל לא / קצת / קורא קבוע
4. כמה קשוח שנהיה איתך? — רך / בינוני / אכזרי
5. מה קורה לדקות שצברת? — מתאפסות בחצות (כל יום מתחילים מחדש) / נשמרות (נצברות בלי תפוגה)

**מסך תוצאה:** המספר במרכז, גדול — "אצלך: כל עמוד = X דקות" + שורת הסבר שקופה ("בחרת לצמצם דרסטית במצב אכזרי — לכן היחס מחמיר") + כפתור "בחירת האפליקציות לחסימה"

### 7.2 בחירת אפליקציות (מדומה)
- כותרת: "מה גוזל לך את הזמן?" · חיווי: **"מומלץ לבחור 2-4 אפליקציות"**
- קיצורים: רשתות חברתיות / גם משחקים / בחירה מותאמת
- רשת אייקונים מדומים (אינסטגרם, טיקטוק, יוטיוב, פייסבוק, X, משחק) עם סימון בחירה
- הערת מימוש בקוד: בגרסת iOS המסך הזה מוחלף ב-FamilyActivityPicker של אפל — לציין בהערה בקובץ
- סיום ⇐ מסך הבית

### 7.3 מסך בית (הטלפון המדומה)
- למעלה: טבעת התקדמות עם יתרת הדקות + שבב סטריק
- רשת אייקוני האפליקציות החסומות; אייקון עם מנעול קטן כשהבנק ריק
- לחיצה על אפליקציה: יש דקות ⇐ "האפליקציה" נפתחת (מסך placeholder עם לוגו + טיימר יורד גדול + כפתור יציאה); אין ⇐ מסך נעילה
- כפתור צף תחתון: "לקריאה 📖" (תמיד זמין — הקריאה לעולם לא חסומה)
- ניווט תחתון: בית · ספרייה · דשבורד · הגדרות

### 7.4 מסך נעילה (הוויזואל השיווקי — להשקיע)
- רקע `--lock` כהה על כל המסך, אייקון מנעול
- "האפליקציה נעולה. אין לך דקות בבנק."
- "עמוד אחד = X דקות" לפי היחס האישי
- כפתור ראשי גדול: "לקריאה" ⇐ הקורא · קישור משני קטן: "חזרה"

### 7.5 הקורא
- epub.js בתצוגת עמודים (לא גלילה), דפדוף בהחלקה ובהקשה על שולי המסך; עמוד הבא = שמאלה
- הקשה במרכז ⇐ סרגלים: למעלה שם היצירה וחזרה; למטה: גודל טקסט (א-/א+), מצב תצוגה (בהיר/כהה/ספיה), מחוון מיקום
- פס עליון דק: עמודים שנקראו היום + "+X דק' לעמוד"; עמוד נספר ⇐ אנימציית +X
- שמירת מיקום אוטומטית (`readingState`); חזרה לספר ממשיכה מאותו מקום
- תומך גם ביצירות בן-יהודה (HTML מומר) וגם ב-EPUB שהמשתמש מייבא (input file)
- סיום יצירה ⇐ מסך "סיימת! 🎉" + סטטיסטיקה קצרה + "לספר הבא" (הרגע היחיד עם קונפטי)

### 7.6 דשבורד
- טבעת יתרה גדולה · היום: עמודים / דקות שנצברו / דקות שנוצלו
- גרף עמודות 7 ימים (SVG פשוט, בלי ספריות) · סטריק נוכחי ושיא · ספרים שהתחלת (עם התקדמות)

### 7.7 הגדרות
- יחס עמוד-דקות: מחוון בטווח **5-30 דק'** (מתחת: "הטווח מוגבל כדי שהמנגנון יישאר אמיתי")
- מצב איפוס: מתאפס בחצות / נשמר (משקף את הבחירה מהשאלון)
- ניהול אפליקציות חסומות (חזרה למסך הבחירה) · מצב תצוגה · גודל טקסט ברירת מחדל
- "אפס הכל" (עם אישור) — לצרכי בדיקות

## 8. מיקרו-קופי (עברית, טון חם ולא מטיף)
- טוסט דפדוף מהיר: "לאט לאט 🙂 עוד רגע העמוד נספר"
- בנק התרוקן: "נגמרו הדקות — עמוד אחד ואתה בפנים"
- בוקר חדש (מצב חצות): "יום חדש, דף חלק. עמוד ראשון?"
- שבירת סטריק — בלי אשמה: "מתחילים רצף חדש היום"

## 9. הגדרת "מוכן לבדיקה" (Definition of Done)
הדמו נחשב גמור רק כשכל אלה עוברים על אייפון אמיתי בספארי:
1. הלולאה המלאה E2E: שאלון ⇐ תוצאה ⇐ בחירת אפליקציות ⇐ בית ⇐ ניסיון פתיחה ⇐ נעילה ⇐ קריאה ⇐ צבירה ⇐ פתיחה עם טיימר ⇐ ניקוז ⇐ נעילה חוזרת
2. אימות: דפדוף מהיר לא צובר; הנחת טלפון 3 דקות עוצרת טיימר; מעבר אפליקציה עוצר מיידית
3. חצות: סימולציית מעבר יום (כפתור dev נסתר "קפוץ יום") מפעילה נכון סטריק/איפוס בשני המצבים
4. קריאה: ספר מהספרייה נפתח בלחיצה, מיקום נשמר אחרי סגירה מלאה של הדפדפן, RTL תקין בדפדוף ובניקוד
5. ייבוא EPUB חיצוני עובד ונשמר
6. עיצוב: שלושת המצבים (בהיר/כהה/ספיה) תקינים בכל המסכים; אין גלישת טקסט; safe-area באייפון; טעינה ראשונה < 2 שניות ברשת סלולרית
7. רענון עמוד בכל נקודה לא מאבד state
8. אין שגיאות console

## 10. סדר בנייה
1. שלד + tokens.css + רכיבי בסיס (כפתור, כרטיס, טבעת)
2. הקורא + verify.js (הלב — ראשון)
3. סקריפט בן-יהודה + קטלוג + מסך ספרייה
4. formula.js + bank.js + streak.js + store.js
5. שאלון + תוצאה + בחירת אפליקציות
6. בית + נעילה + אפליקציה מדומה עם טיימר — סגירת הלולאה
7. דשבורד + הגדרות
8. ליטוש, מיקרו-קופי, מעבר על כל סעיף 9

## 11. מחוץ לסקופ (לא לבנות)
- חסימה אמיתית (Screen Time API / Swift) — שלב ה-Mac, ייבנה שם ראשון כאב-טיפוס מבודד
- מנויים/תשלומים (RevenueCat) · שאלות הבנה על הנקרא (גרסה 2) · אנדרואיד · שרת/חשבונות · התראות push
