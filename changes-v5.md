# שינויים — V5: אנדרואיד, חסימה אמיתית והעלאה ל-Play

> מקורות האמת נשארים `brief-v2-multi-niche.md` + `changes-v3.md` + `changes-v4.md`; המסמך הזה מוסיף עליהם את שכבת האנדרואיד.
> **החלטה שנקבעה:** הגרסה הראשונה ב-Play כוללת **חסימה אמיתית**. בלי זה האפליקציה מבטיחה משהו שהיא לא עושה.
> אייפון — רק אחרי שרואים ביקוש.

---

## א. ההחלטה שקובעת את כל השאר

הקוד הקיים — 40 קבצי JS, ~7,900 שורות — יעבוד באנדרואיד כמעט כמו שהוא. **הדבר היחיד שאפליקציית ווב לא יכולה לעשות הוא בדיוק הלב של המוצר: לחסום אפליקציות.**

באנדרואיד אין API רשמי לחסימה. כל החוסמים האמיתיים בנויים משני חלקים:

1. **`UsageStatsManager`** — לזהות איזו אפליקציה עלתה לחזית
2. **`SYSTEM_ALERT_WINDOW`** — לצייר מסך נעילה מעליה

שתיהן הרשאות מיוחדות שהמשתמש נותן ידנית ב-Settings, ושתיהן קוד נייטיב (Kotlin) — לא JS.

### א1. `AccessibilityService` — לא ללכת לשם

זו הדרך הקלה טכנית, והיא מלכודת. מדיניות Play מגבילה את ה-API הזה לשימושי נגישות אמיתיים, ואפליקציות רווחה דיגיטלית שהשתמשו בו הוסרו מהחנות. **אנחנו לא נוגעים בו.**

### א2. Capacitor ולא TWA

- **TWA** עוטף PWA בקליפה, אבל אין לו גישה להרשאות מיוחדות ואי אפשר לכתוב בו תוסף נייטיב. **לא מתאים.**
- **Capacitor** עוטף את אותו אתר סטטי, ומאפשר לכתוב תוסף Kotlin משלנו. **זו הבחירה.**

אין build step היום, וזה יתרון: `webDir` מצביע על שורש הפרויקט, ו-Capacitor מעתיק אותו כמו שהוא אל `android/app/src/main/assets/public`.

---

## ב. שכבת החסימה (הלב של הסבב)

### ב1. הבעיה הארכיטקטונית שחייבים לפתור ראשונה

**שירות החסימה רץ כשה-WebView סגור.** הוא לא יכול לקרוא ל-JS, ולא יכול לקרוא את `localStorage`.

היום כל מצב האפליקציה — ובעיקר **יתרת הבנק** — חי ב-`localStorage`. השירות חייב לדעת כמה דקות נשארו כדי להחליט אם לחסום.

**הפתרון:** מראה נייטיב. כל שינוי בבנק נדחף מיד ל-`SharedPreferences`:

- `store.js` מקבל hook יחיד — `mirrorToNative()` — שנקרא מ-`setBank` בלבד (אותו עיקרון של נקודת זיכוי יחידה).
- התוסף חושף `setBankMinutes(n)` ו-`setBlockedPackages([...])`.
- מקור האמת נשאר ה-JS; הנייטיב הוא מראה לקריאה בלבד.
- ההפך גם נדרש: כשהמשתמש "מוציא" דקות בזמן שהאפליקציה סגורה, השירות מנכה מהמראה, וב-`openDay` הבא ה-JS מסנכרן את עצמו ממנה.

**זה הסעיף המסוכן ביותר במסמך.** שני מקורות אמת לאותו מספר זו הדרך הקלאסית לאבד דקות של משתמשים. להגדיר בכתב, לפני הקוד, מי מנצח בכל התנגשות.

### ב2. התוסף — `DafdefBlocker`

תוסף Capacitor ב-Kotlin, עם API מצומצם:

```
hasUsageAccess() / requestUsageAccess()
hasOverlayPermission() / requestOverlayPermission()
isIgnoringBatteryOptimizations() / requestIgnoreBatteryOptimizations()
listLaunchableApps()        → [{ packageName, label, icon }]
setBlockedPackages([...])
setBankMinutes(n)
startService() / stopService()
getState()                  → { running, permissions, bankMinutes }
```

### ב3. השירות

- **Foreground Service** עם התראה קבועה (חובה, ובלעדיה המערכת הורגת אותו).
- אנדרואיד 14+ דורש `foregroundServiceType`; הסוג המתאים הוא **`specialUse`**, שדורש הצהרה ב-Play.
- לולאת בדיקה: `UsageStatsManager.queryEvents` ל-`MOVE_TO_FOREGROUND`, בערך פעם בשנייה. **לכייל מול צריכת סוללה** — זה מה שיקבע אם משתמשים משאירים את האפליקציה מותקנת.
- `RECEIVE_BOOT_COMPLETED` כדי לחזור אחרי הפעלה מחדש.
- בקשת פטור מאופטימיזציית סוללה, עם הסבר למה.

### ב4. מסך הנעילה

- כשאפליקציה חסומה עולה לחזית **והבנק ריק** → מציגים מסך נעילה.
- **הערה טכנית חשובה:** הפעלת Activity מהרקע חסומה מאנדרואיד 10, **אבל הרשאת `SYSTEM_ALERT_WINDOW` פותחת אותה שוב**. זו הסיבה שההרשאה הזו הכרחית ולא רק "נחמדה".
- שתי אפשרויות למימוש — להחליט בזמן הבנייה: Activity מלאה, או overlay דרך `WindowManager` עם `TYPE_APPLICATION_OVERLAY`. Overlay קל יותר להחזיר ממנו את המשתמש למקום שהיה בו.
- על המסך: כמה דקות בבנק (0), מה המשימה הבאה, וכפתור שפותח את דפדף.
- **יש דקות בבנק** → לא חוסמים, אלא מתחילים לנכות: דקה אחת לכל דקת שימוש, והמראה מתעדכנת.

### ב5. רשימת האפליקציות לחסימה

- היום `onboarding.html` מציג רשימה מדומה. באנדרואיד — האפליקציות שמותקנות באמת.
- **להימנע מ-`QUERY_ALL_PACKAGES`** — היא הרשאה רגישה שדורשת הצהרה ואישור ב-Play. במקום זה: אלמנט `<queries>` ב-manifest עם סינון `ACTION_MAIN` + `CATEGORY_LAUNCHER`, שמספיק כדי לקבל את כל האפליקציות שיש להן אייקון, ואינו דורש הרשאה רגישה.

---

## ג. אריזה — מה חייב לרדת מה-CDN

היום נטענים מהרשת בכל הפעלה:

| מה | גודל | ל-assets? |
|---|---|---|
| MediaPipe tasks-vision + wasm | ~9MB | **חובה** |
| מודלי pose / face / object detector | ~8MB | **חובה** |
| epub.js + jszip | ~1MB | חובה |
| גופני Heebo ו-Frank Ruhl Libre | ~200KB | חובה |

באפליקציה מותקנת, טעינה מהרשת היא **מסך המתנה בכל פתיחה** ותקלה מוחלטת בלי אינטרנט. הכל יורד לתוך הפרויקט, והנתיבים ב-`camera.js` ובקורא משתנים ל-מקומיים.

תופעת לוואי טובה: זה מסיר את התלות ב-CDN גם באתר הרגיל.

**להחליט:** האם לארוז את כל שלושת המודלים (נפח APK גדול) או להוריד מודל לפי הצורך בהפעלה הראשונה. המלצה: לארוז — הפעם הראשונה היא הרגע שבו מאבדים משתמשים.

---

## ד. Health Connect — צעדים ושינה אמיתיים

היום `background.js` עובד על נתונים מדומים עם כלי dev להזרקה.

- ספרייה: `androidx.health.connect:connect-client`
- הרשאות: `android.permission.health.READ_STEPS`, `READ_SLEEP`
- באנדרואיד 14+ Health Connect מובנה במערכת; בגרסאות קודמות זו אפליקציה מ-Play שצריך לוודא שקיימת
- Play דורש **טופס הצהרה** לשימוש בנתוני בריאות
- `background.js` עצמו לא משתנה — רק המקור שמזין אותו. הלוגיקה הטהורה נשארת, וכך גם הבדיקות

---

## ה. הרשאות וזרימת ההדרכה

זו נקודת הנטישה הגדולה ביותר. המשתמש צריך לתת **שלוש-ארבע הרשאות מיוחדות**, כל אחת במסך Settings נפרד, כשאף אחת מהן אינה דיאלוג רגיל.

- מסך הסבר לפני כל בקשה: **למה** צריך אותה ומה קורה בלעדיה
- לבקש אותן **אחת-אחת ובסדר**, לא בבת אחת
- מצב חלקי הוא מצב תקין: בלי Usage Access אין חסימה אבל האפליקציה עובדת — להגיד את זה ולא להיתקע
- מסך סטטוס קבוע בהגדרות: מה ניתן, מה חסר, וכפתור לתקן
- לא לבקש Health Connect בהתחלה — רק אם המשתמש בחר צעדים או שינה

---

## ו. מה משתנה בקוד הקיים

רוב הקוד לא נוגע. השינויים:

1. **`store.js`** — מראה נייטיב לבנק ולאפליקציות החסומות (ב1)
2. **`camera.js`** — נתיבים מקומיים למודלים ול-wasm במקום CDN
3. **`reader.js`** — epub.js מקומי
4. **כל ה-HTML** — גופנים מקומיים
5. **`onboarding.html`** — רשימת אפליקציות אמיתית מהתוסף
6. **`background.js`** — מקור Health Connect במקום הדמה
7. **`settings.html`** — מסך סטטוס הרשאות
8. **חדש:** `manifest.json`, service worker, סט אייקונים, `privacy.html`

---

## ז. Play Console — מה שצריך לדעת מראש

- **עלות:** ~$25 חד-פעמי
- **חשבון אישי חדש:** נדרשת בדיקה סגורה עם **12 בודקים פעילים במשך 14 יום ברציפות** לפני שאפשר לבקש גישה לייצור. **לפתוח את החשבון ולהתחיל את הבדיקה הסגורה במקביל לפיתוח, לא אחריו** — אחרת זה שבועיים מתים בסוף
- **מדיניות פרטיות** — חובה, כתובת URL ציבורית. צריך לכתוב `privacy.html` (אין היום)
- **Data Safety** — טופס שמצהיר מה נאסף. המצלמה רצה כולה על המכשיר ואין העלאה, וזה יתרון שכדאי לנסח היטב
- **הצהרות נדרשות:** `foregroundServiceType=specialUse`, נתוני בריאות
- **Target API level** — Play דורש גרסה עדכנית לאפליקציות חדשות

> מדיניות Play משתנה. לאמת כל סעיף כאן מול המסמכים הרשמיים בזמן ההגשה ולא להסתמך על המספרים האלה כלשונם.

---

## ח. מה לא בסבב הזה

- **אייפון.** ב-iOS החסימה עוברת דרך Screen Time API (`FamilyControls` / `ManagedSettings`), שהוא מודל אחר לגמרי ודורש Mac. רק אחרי שרואים ביקוש
- **שרת וחשבונות.** הליגה נשארת דמו מקומי (V4, סעיף 2.2)
- **סליקה ומנויים**
- **התראות push**

---

## ט. סדר ביצוע

1. **אריזה** — מורידים את MediaPipe, epub.js והגופנים לפרויקט; manifest, service worker, אייקונים. *נדרש בכל מקרה ולא תלוי בשום החלטה*
2. **Capacitor** — מתקינים, `webDir` לשורש, מוודאים שהאפליקציה עולה במכשיר אמיתי עם כל הבדיקות ירוקות
3. **מראה הבנק** (ב1) — לפני כל קוד חסימה. בלי זה אין על מה לבנות
4. **התוסף** — הרשאות, `listLaunchableApps`, סטטוס
5. **השירות ומסך הנעילה** — כולל כיול סוללה
6. **רשימת אפליקציות אמיתית** באונבורדינג
7. **Health Connect**
8. **מסך סטטוס הרשאות** בהגדרות + זרימת ההדרכה
9. **`privacy.html`** + טפסי Play
10. **בדיקה סגורה** — להתחיל מוקדם ככל האפשר (ז)

---

## י. הגדרת סיום

- האפליקציה נפתחת בלי אינטרנט, והמצלמה עובדת בלי הורדה
- אפליקציה חסומה + בנק ריק ⇐ מסך נעילה תוך שנייה
- אפליקציה חסומה + יש דקות ⇐ נכנסים, והדקות יורדות בקצב אמיתי
- הבנק תואם בין ה-JS לנייטיב אחרי: סגירת האפליקציה, ניכוי ברקע, ופתיחה מחדש
- השירות שורד הפעלה מחדש של המכשיר
- צריכת הסוללה נמדדה ואינה חריגה
- צעדים ושינה מגיעים מ-Health Connect אצל מי שבחר אותם
- כל ההרשאות מוסברות לפני הבקשה, ומצב חלקי אינו שובר כלום
- כל הבדיקות האוטומטיות הקיימות ממשיכות לעבור
